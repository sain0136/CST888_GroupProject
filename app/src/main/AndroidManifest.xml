<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cst2335.soccerapi">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SoccerAPI">
        <activity android:name=".FavoriteNews"></activity>
        <activity android:name=".SoccerMain"></activity>
        <activity android:name=".EmptyActivity"></activity>
        <activity android:name=".EmptyFrameWork"></activity>
        <activity android:name=".MovieActivity"></activity>
        <activity android:name=".SavedMovies"></activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>